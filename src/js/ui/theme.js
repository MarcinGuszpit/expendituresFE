import {createTheme} from "@mui/material";

export const app_theme = createTheme({
    palette: {
        mode: 'light',
        primary: {
            main: '#be9e82',
            contrastText: '#fff',
            dark: '#40434e'
        },
        secondary: {
            main: '#e4e4e4',
        },
    }
});
